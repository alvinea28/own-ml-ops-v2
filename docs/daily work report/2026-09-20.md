# 2026-09-20 - Use the standalone repaired taxi repository

## Completed

- Replaced the proposed overlay design with a separate private GitHub snapshot:
  [alvinea28/taxi-fare-regression](https://github.com/alvinea28/taxi-fare-regression).
- Updated initialization on this accelerator's `dev` branch to select the snapshot's
  Azure Repos import, `taxi-fare-regression-template`, only for classical / AML CLI
  v2 / Bicep. Other variants and the explicit opt-out retain the upstream template.
- Used fixed checkout locations and environment-bound arguments rather than
  constructing shell commands from project/repository parameters.
- Preserved input checkouts; initialization copies files without moving or deleting
  source content and never writes global Git identity settings.
- Added new-project-only safeguards for application files, dirty targets, prior
  history, shallow checkouts, and overlapping directories. Full target history is
  required and the real shallow-clone regression was proved red before the fix.
- Pipeline registration now discovers actual top-level YAML entry points, not the
  taxi project's reusable `templates` folder, and propagates command failures.
- Existing untracked `project-overrides` files contain distinct earlier edits and
  were preserved locally, unused and excluded from the publication commit.
- No changes were made to the protected working taxi repository or this repository's
  upstream `main` branch. The new standalone repo has its own independent `main`.

## Verification

- Initializer suite: **20 passed**, no failures/skips, using disposable local Git
  remotes and mocked Azure pipeline creation.
- Actual generated taxi project: **34 Node/Bash/CLI** and **18 Python** regression
  cases passed, including MLflow artifacts, model promotion, and native WSGI scoring.
- A fresh Python 3.11.14 environment resolved **88 compatible packages** outside
  both repositories; the protected project's virtual environment was not used.
- Static checks parsed **22 YAML**, **8 JSON**, and **10 Python** publication inputs;
  code-map references were validated and token/personal-setting scans found no match.
- Protected working repository: clean source commit
  `7223bcab85b9896474258be04dce0e7b08bff6a5` and **52 file/Git-metadata fingerprints**
  matched the initial baseline before publication.
- Focused independent code review: **APPROVE** after the shallow-history correction.

## Documentation

- [Updated taxi integration guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/docs/TAXI-TEMPLATE.md)
- [Accelerator README](https://github.com/alvinea28/own-ml-ops-v2/blob/main/README.md)
- [This report](https://github.com/alvinea28/own-ml-ops-v2/blob/main/docs/daily%20work%20report/2026-09-20.md)
- [Standalone taxi README](https://github.com/alvinea28/taxi-fare-regression/blob/main/README.md)
- [Standalone rerun guide](https://github.com/alvinea28/taxi-fare-regression/blob/main/docs/online-deployment-rerun.md)
- [Standalone publication report](https://github.com/alvinea28/taxi-fare-regression/blob/main/docs/daily%20work%20report/2026-09-20.md)

## Remaining setup and limits

Import/synchronize the personal accelerator's `main` branch and the taxi snapshot
into Azure Repos, then initialize a **new** target repository. Private GitHub access
must be granted separately; no collaborator or service-connection permissions were
changed. The shared `mlops-templates` repository is still required.

The generated project still requires its own Azure configuration, quotas, and
compute identity/registry pull permissions. No Azure deployment, DevOps-hosted
pipeline run, endpoint health check, or production traffic change was performed.
No new live application URL or hosted health result is claimed.

## Follow-up: use main instead of dev

At the user's request, promoted the complete reviewed `dev` tree to personal
`main` by fast-forward, without rewriting history or deleting `dev`. `main` is
the publication and default branch for the repaired accelerator. The earlier
`dev`-publication notes above describe the first publication before this change.

Updated the README, current setup guide, and historical study notice to point to
`main`; the standalone taxi repository's setup links were updated to match. The
initializer, its tests, and all application code are unchanged in this follow-up.
Existing untracked overlay work and the original working taxi repository remain
untouched. The earlier test results above are retained, not presented as a new
cloud deployment run.

The Azure Repos import must contain the new personal `main` branch. Then select
`main` manually when configuring or running the initialization pipeline; the
GitHub default-branch setting cannot change an existing Azure Repos pipeline.

## Follow-up: personal documentation URLs and PDF

- Updated the Azure DevOps guide, GitHub guide, and repository-structure guide so
  accelerator clone/import, prerequisites, and support links point to
  `alvinea28/own-ml-ops-v2`, not `Azure/mlops-v2`.
- The repaired taxi walkthrough now imports `alvinea28/taxi-fare-regression` as
  `taxi-fare-regression-template`, documents the actual initializer parameters,
  and requires `main`. Microsoft Learn links and genuine upstream helper/template
  dependencies are retained; personal GitHub access and import synchronization
  requirements are explicit.
- Exported the updated guide as a tagged, searchable **32-page PDF**, with all
  **40 screenshots**, **3 expanded detail sections**, **30 headings**, and
  **36 external links** verified. No broken screenshots, local-file hyperlinks,
  or text outside the page bounds were found. Rendering used embedded local
  images and made no network requests.
- Temporary Markdown/Edge/PDF.js tools were isolated outside the repository.
  No application code, pipeline execution, Azure resources, or original working
  taxi repository was changed. Original source screenshots retain upstream labels.

PDF: [Download the Azure DevOps deployment guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.pdf).
Source: [Updated Markdown guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.md).

- PDF size: **3,228,916 bytes**.
- PDF SHA-256: `089f57dae11dd62225eeae07f37c903413251b18998f0e65a49e4d7d840308e0`.
- LF-normalized Markdown SHA-256: `8734b80fe50e71fa1094b405e8bb73a6bb3e84d3021dcf02ae082168a3aacc62`.

## Follow-up: remove OpenCode and keep future work lightweight

- Published the guide/PDF in commit `63fec2ffa23c86ecf7493a497b97270710fc8f29`.
- Removed the tracked `.opencode` directory from the current branch in the
  succeeding cleanup commit; prior Git history is not rewritten.
- Added `.opencode/` to Git ignore rules and removed its file-formatting rule.
- Added [repository-wide Copilot preferences](https://github.com/alvinea28/own-ml-ops-v2/blob/main/.github/copilot-instructions.md)
  to prevent recreation of OpenCode/Graphify artifacts and prohibit custom Scrum
  or full-team orchestration. Standard focused subagents remain optional.
- Kept the published PDF and runtime files unchanged. Earlier validation and
  historical reports describe the state when recorded; they do not require a
  current repository knowledge map. Unrelated untracked override files are preserved.
