# 2026-09-20 - Use the standalone repaired taxi repository

## Completed

- Replaced the proposed overlay design with a separate private GitHub snapshot:
  [alvinea28/taxi-fare-regression](https://github.com/alvinea28/taxi-fare-regression).
- Updated initialization on this accelerator's `dev` branch to select the snapshot's
  Azure Repos import, `taxi-fare-regression-template`, only for classical / AML CLI
  v2 / Bicep. Other variants and the explicit opt-out retain the upstream template.
- Used fixed checkout locations and environment-bound arguments rather than
  constructing shell commands from project/repository parameters.
- Preserved input checkouts; initialization copies files without moving or deleting
  source content and never writes global Git identity settings.
- Added new-project-only safeguards for application files, dirty targets, prior
  history, shallow checkouts, and overlapping directories. Full target history is
  required and the real shallow-clone regression was proved red before the fix.
- Pipeline registration now discovers actual top-level YAML entry points, not the
  taxi project's reusable `templates` folder, and propagates command failures.
- Existing untracked `project-overrides` files contain distinct earlier edits and
  were preserved locally, unused and excluded from the publication commit.
- No changes were made to the protected working taxi repository or this repository's
  upstream `main` branch. The new standalone repo has its own independent `main`.

## Verification

- Initializer suite: **20 passed**, no failures/skips, using disposable local Git
  remotes and mocked Azure pipeline creation.
- Actual generated taxi project: **34 Node/Bash/CLI** and **18 Python** regression
  cases passed, including MLflow artifacts, model promotion, and native WSGI scoring.
- A fresh Python 3.11.14 environment resolved **88 compatible packages** outside
  both repositories; the protected project's virtual environment was not used.
- Static checks parsed **22 YAML**, **8 JSON**, and **10 Python** publication inputs;
  code-map references were validated and token/personal-setting scans found no match.
- Protected working repository: clean source commit
  `7223bcab85b9896474258be04dce0e7b08bff6a5` and **52 file/Git-metadata fingerprints**
  matched the initial baseline before publication.
- Focused independent code review: **APPROVE** after the shallow-history correction.

## Documentation

- [Updated taxi integration guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/docs/TAXI-TEMPLATE.md)
- [Accelerator README](https://github.com/alvinea28/own-ml-ops-v2/blob/main/README.md)
- [This report](https://github.com/alvinea28/own-ml-ops-v2/blob/main/docs/daily%20work%20report/2026-09-20.md)
- [Standalone taxi README](https://github.com/alvinea28/taxi-fare-regression/blob/main/README.md)
- [Standalone rerun guide](https://github.com/alvinea28/taxi-fare-regression/blob/main/docs/online-deployment-rerun.md)
- [Standalone publication report](https://github.com/alvinea28/taxi-fare-regression/blob/main/docs/daily%20work%20report/2026-09-20.md)

## Remaining setup and limits

Import/synchronize the personal accelerator's `main` branch and the taxi snapshot
into Azure Repos, then initialize a **new** target repository. Private GitHub access
must be granted separately; no collaborator or service-connection permissions were
changed. The shared `mlops-templates` repository is still required.

The generated project still requires its own Azure configuration, quotas, and
compute identity/registry pull permissions. No Azure deployment, DevOps-hosted
pipeline run, endpoint health check, or production traffic change was performed.
No new live application URL or hosted health result is claimed.

## Follow-up: use main instead of dev

At the user's request, promoted the complete reviewed `dev` tree to personal
`main` by fast-forward, without rewriting history or deleting `dev`. `main` is
the publication and default branch for the repaired accelerator. The earlier
`dev`-publication notes above describe the first publication before this change.

Updated the README, current setup guide, and historical study notice to point to
`main`; the standalone taxi repository's setup links were updated to match. The
initializer, its tests, and all application code are unchanged in this follow-up.
Existing untracked overlay work and the original working taxi repository remain
untouched. The earlier test results above are retained, not presented as a new
cloud deployment run.

The Azure Repos import must contain the new personal `main` branch. Then select
`main` manually when configuring or running the initialization pipeline; the
GitHub default-branch setting cannot change an existing Azure Repos pipeline.

## Follow-up: personal documentation URLs and PDF

- Updated the Azure DevOps guide, GitHub guide, and repository-structure guide so
  accelerator clone/import, prerequisites, and support links point to
  `alvinea28/own-ml-ops-v2`, not `Azure/mlops-v2`.
- The repaired taxi walkthrough now imports `alvinea28/taxi-fare-regression` as
  `taxi-fare-regression-template`, documents the actual initializer parameters,
  and requires `main`. Microsoft Learn links and genuine upstream helper/template
  dependencies are retained; personal GitHub access and import synchronization
  requirements are explicit.
- Exported the updated guide as a tagged, searchable **32-page PDF**, with all
  **40 screenshots**, **3 expanded detail sections**, **30 headings**, and
  **36 external links** verified. No broken screenshots, local-file hyperlinks,
  or text outside the page bounds were found. Rendering used embedded local
  images and made no network requests.
- Temporary Markdown/Edge/PDF.js tools were isolated outside the repository.
  No application code, pipeline execution, Azure resources, or original working
  taxi repository was changed. Original source screenshots retain upstream labels.

PDF: [Download the Azure DevOps deployment guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.pdf).
Source: [Updated Markdown guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.md).

- PDF size: **3,228,916 bytes**.
- PDF SHA-256: `089f57dae11dd62225eeae07f37c903413251b18998f0e65a49e4d7d840308e0`.
- LF-normalized Markdown SHA-256: `8734b80fe50e71fa1094b405e8bb73a6bb3e84d3021dcf02ae082168a3aacc62`.

## Follow-up: remove OpenCode and keep future work lightweight

- Published the guide/PDF in commit `63fec2ffa23c86ecf7493a497b97270710fc8f29`.
- Removed the tracked `.opencode` directory from the current branch in the
  succeeding cleanup commit; prior Git history is not rewritten.
- Added `.opencode/` to Git ignore rules and removed its file-formatting rule.
- Added [repository-wide Copilot preferences](https://github.com/alvinea28/own-ml-ops-v2/blob/main/.github/copilot-instructions.md)
  to prevent recreation of OpenCode/Graphify artifacts and prohibit custom Scrum
  or full-team orchestration. Standard focused subagents remain optional.
- Kept the published PDF and runtime files unchanged. Earlier validation and
  historical reports describe the state when recorded; they do not require a
  current repository knowledge map. Unrelated untracked override files are preserved.

## Follow-up: locate the PDF and refresh remaining Markdown links

- Confirmed the PDF is committed on `main` at
  [documentation/deployguides/deployguide_ado.pdf](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.pdf),
  originally added by commit `63fec2ffa23c86ecf7493a497b97270710fc8f29`.
- Added explicit personal-repository `main` links for both Markdown and PDF in the
  root README, deployment-guide index, and taxi setup notes.
- Updated the September 19 report's document URLs from `dev` to `main`, removed
  its obsolete current-map link, and labeled past branch state as historical.
- Retained the explicitly dated upstream study as a historical record, including
  original Azure URLs that genuinely identify upstream provenance rather than
  current clone/import destinations.
- Audited the standalone taxi copy's Markdown cross-links: they already point to
  the personal accelerator's `main`; no changes there were needed.
- The guide source and PDF bytes are unchanged, so the validated 32-page PDF
  remains synchronized with its source. No runtime or original taxi files were edited.

## Follow-up: explain imports and generated project ownership

- Verified the actual personal GitHub inventory: `own-ml-ops-v2` and
  `taxi-fare-regression` exist; the two upstream companion repositories were not
  copied into this account. No extra repository or fork was created.
- Clarified that `taxi-fare-regression-template` is an imported reusable source,
  while a new empty target such as `taxi-fare-regression-demo` is populated by the
  initializer. The target Azure Repos repository must already exist for checkout.
- Documented that `mlops-templates` remains required and `mlops-project-template`
  is optional for other scenarios, and that GitHub ownership does not eliminate
  Azure Repos imports under the current `git://` checkout design.
- Observed that the personal accelerator is now public while the taxi source
  remains private. This was existing account state, not a visibility change made
  by this task; access guidance now avoids assuming both have identical visibility.

## Follow-up: put the clarification in the requested guide itself

- The earlier source/target clarification was in the separate setup notes. Updated
  [the exact Azure DevOps guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.md#what-is-imported-and-what-does-the-initializer-create)
  requested by the user, with a prominent section before the prerequisites.
- Added source-versus-target roles, the initializer's actual copy/commit behavior,
  the requirement to create an empty target first, and an explicit statement that
  the upstream companion repositories have not been copied into the personal account.
- Repeated the distinction at the import steps, new-repository step, and `repoName`
  parameter. Corrected the old claim that both personal repositories are private.
- Regenerated the PDF from this exact updated Markdown. This **33-page refresh**
  supersedes the earlier 32-page PDF and hashes recorded above.
- Validated **40 screenshots**, **3 expanded sections**, **31 headings**, **42 links**,
  and **10 required clarification/content checks**. No broken images, missing
  internal anchors, local-file URLs, or overflowing text were found. The opening
  page was also reviewed visually; rendering used local images with networking blocked.
- Runtime files, original working taxi files, repository visibility, and the
  unrelated local override directory were not changed. No OpenCode artifacts were recreated.

Current PDF: [Azure DevOps deployment guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.pdf).

- PDF size: **3,251,598 bytes**.
- PDF SHA-256: `6ef99a6a94c62246397b5672c240ddabcd7a521646d21c4003b4b6c08794a9fb`.
- LF-normalized Markdown SHA-256: `bb35b0755cbe55009f512e941b1bd490c07b0ce62d51f5cd1e025d037027f5b2`.

## Follow-up: simplify the Run pipeline form

- Simplified [the default taxi initializer](https://github.com/alvinea28/own-ml-ops-v2/blob/main/.azuredevops/initialise-project.yml)
  to exactly three inputs: `adoProjectName`, `repoName`, and `taxiTemplateRepoName`.
  Display names distinguish the Azure DevOps project, already-created destination,
  and already-imported source. Classical ML, AML CLI v2, Bicep, and the repaired
  standalone template layout are fixed automatically.
- Removed the repaired-template checkbox, unused upstream-template field, and
  workload/interface/provider choices from the normal form. Added
  [a separate advanced upstream initializer](https://github.com/alvinea28/own-ml-ops-v2/blob/main/.azuredevops/initialise-project-advanced.yml)
  with only its six relevant inputs. It preserves the upstream nested-template
  path without implying it contains the repaired standalone taxi code.
- Kept the existing shell scripts, target-history protection, source isolation,
  environment-bound arguments, and first-run-disabled pipeline registration intact.
- Updated the exact Azure DevOps guide, setup notes, README, and structure notes.
  The old parameter screenshot was omitted because it shows obsolete controls.
  The guide explains `fixed-mlops-v2`, `taxi-fare-regression-demo`, and
  `taxi-fare-regression-template`, and how to refresh an existing Azure Repos import.

Validation:

- Three targeted tests reproduced the old form problem before the fix.
- Final captured run: **26 initializer tests passed**, **0 failures**, **0 skips**,
  including actual inline YAML wrapper execution for both entry points and a
  generated real taxi snapshot running its **34 Node pipeline checks**.
- Both YAML files parsed with duplicate-key checking; the standard form exposes
  **3** parameters and the advanced form **6**, with no undeclared references.
- A standard/default read-only subagent approved the change. No Scrum or custom
  agent orchestration was used.
- Refreshed PDF: **32 pages**, **39 screenshots**, **3 expanded sections**,
  **31 headings**, **45 links**, and **10 updated-content checks**, with no broken
  images, missing internal anchors, local-file links, or overflowing text.
- Python model tests were not rerun because model/runtime code did not change.
  No Azure pipeline was queued. To see the new form, synchronize GitHub `main`
  into Azure Repos, keep the default taxi YAML path, and reopen **Run pipeline**.
- The original working taxi repository and standalone source copy remain untouched.
  Pre-existing local study edits and untracked override files are excluded from
  publication. OpenCode artifacts are not recreated.

This export supersedes the earlier PDF metrics in this report:

- PDF size: **3,209,266 bytes**.
- PDF SHA-256: `cdb17fe5db28c82618b7c8c810478319e40a3b723cba803a6283ab2d198f31e9`.
- LF-normalized Markdown SHA-256: `9bfd298bb7085066963f434be456d967a93ddf995c6c74df3caad5fe0d3a7921`.

## Follow-up: repair the missing source and run initialization

- Confirmed the reported source-checkout failure against the actual Azure Repos
  inventory: the repaired source repository was missing, while the manually
  created destination contained only its initial README. The generic upstream
  project-template repository did not replace the missing repaired source.
- Created the separate source import from the exact repaired GitHub snapshot
  `4914a352941b168c8af509e880e4b28b7fb7329d`. Both local taxi repositories and the
  original working application's files remained unchanged.
- Fast-forwarded the stale Azure Repos accelerator to the reviewed current code,
  then to `7b1c270884f7e337486e254bb861c8184bcb11b9`. No history was rewritten.
- Authorized only the initializer pipeline for its source and target repositories;
  did not disable repository protection or grant access to all pipelines.
- Added a source-existence comment to the YAML and excluded retired assistant
  metadata from generated files without editing that metadata in the source.
  All **26 initializer regressions** and the generated taxi's **34 Node pipeline
  checks** passed, including preservation of the source and metadata exclusion.

Live result:

- **Initialization run 31 succeeded** using the corrected initializer. Both the
  repository-population task and pipeline-registration task succeeded.
- The generated commit `4b2536fe7e2f2590103e75eee5c95b57449b6984` retains the original
  empty-target README commit as its parent. The destination was filled by the
  initialization pipeline, not by a manual copy into the application repository.
- Verified **54 generated files** against the source's Git blob IDs: **zero
  mismatches and zero extras** after excluding retired assistant metadata.
- Verified **four pipeline definitions**: infrastructure, model training, online
  deployment, and batch deployment. Their first runs were skipped; the subsequent
  build inventory showed **zero runs** for these four pipelines. No ML training,
  endpoint deployment, or Azure infrastructure deployment was started.

Documentation now includes a pre-run existence/name/branch checklist, project
Build Service and pipeline-authorization checks, and safe retry guidance that
does not tell readers to delete working repositories or Azure resources.
The intended workflow remains: create an empty destination, then let the
initializer fill it from the separate fixed source.

Refreshed PDF validation: **33 pages**, **39 screenshots**, **3 expanded sections**,
**33 headings**, **47 links**, and **9 required-content checks**; no broken images,
missing internal anchors, local-file links, or overflowing text.

- PDF size: **3,225,361 bytes**.
- PDF SHA-256: `14b470a080a9811953e75d4501f1314ce4452dae6ab861ed09b856d9965333331`.
- LF-normalized Markdown SHA-256: `5f687e855beef814d5ca2139030282739f75d0e3cf61687ca868b3a920c5fd4c`.

## Follow-up: explicit taxi-template import instructions

- Added a dedicated [Import the taxi regression template](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.md#import-the-taxi-regression-template)
  section in the exact Azure DevOps guide, before the pre-run repository checklist.
- Documented **Repos → Files → repository dropdown → Import repository**, the exact
  clone URL **https://github.com/alvinea28/taxi-fare-regression.git**, and the Azure
  Repos name **taxi-fare-regression-template**. The suffix is an Azure Repos name,
  not part of the GitHub repository name or URL.
- Added `main`-branch and source-file checks, conditional authentication guidance,
  and pipeline authorization reminders for newly imported/recreated repositories.
  The imported source must be kept when resetting demo destinations; the original
  working project must not be used as the target or deleted.
- Linked the same steps from the setup notes and guide index. Corrected the stale
  private-source statement: GitHub now reports the taxi source as public. No
  visibility or permission settings were changed by this documentation task.
- No repository was imported, deleted, or recreated in Azure DevOps, and no
  pipeline was run during this follow-up; the user can perform the documented steps.

The refreshed PDF contains **34 pages**, **39 screenshots**, **3 expanded sections**,
**34 headings**, and **47 links**. Ten import-specific content checks passed,
including the exact clone URL, destination/source distinction, and authorization
reminder. All screenshots loaded; no missing anchors, local-file URLs, or text
overflow were found. Temporary rendering dependencies remain outside the repo.

- [Updated PDF](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.pdf)
- PDF size: **3,249,972 bytes**.
- PDF SHA-256: `5ace080bef419aa3c307d01c3e79b7524a83c2d3896dfbc31257ec8be40e9778`.
- LF-normalized Markdown SHA-256: `1bb68bcc5dfc37e4107aa89d86b1375873d7bf600e42f2adb655b8989c901a5f`.

## Follow-up: template-first ESv3 VM sizes and quota instructions

- Published the VM-family change in the reusable taxi GitHub template first:
  commit `7fd43168ea621bb66e7c537dcc328b42b4f9f2c0`. Fast-forwarded its actual Azure
  Repos import, currently named `taxi-fare-regression.git`, to the same revision.
- Applied that tested change to the user's generated **taxi-fare-demo** repository
  in **fixed-mlops-v2**, commit `d5a86839ea9c1aa4f553a47fb2647e5aa391f002`.
  Original working taxi files, model code, data, environment names, regions, and
  service-connection bindings were preserved.
- Defaults are `Standard_E4s_v3`, Dedicated, 0–4 nodes for Bicep/training;
  `Standard_E4s_v3`, Dedicated, 0–5 for batch; and `Standard_E2s_v3`, one instance
  online. The manual batch definition and compiled ARM snapshot are aligned.
- Both environment files provide shared SKU/scaling variables. The infrastructure
  validation and deployment tasks and both compute helper callers consume them;
  compile-time expressions preserve numeric parameter types.
- Independently compiled Bicep and passed **44 Node tests** in each code repository
  (34 existing checks plus 10 compute-profile regressions). YAML parsing/resolution
  and 13 matching compute/profile files were verified across both copies. The stale
  ARM regression was proved red before regeneration. Existing storage/registry
  name-length warnings remain; no new compute compilation errors were found.
- A standard/default read-only review approved the scoped changes. No Scrum or
  custom agents were used. No Azure deployment/training pipeline was queued, no
  quota change was requested, and no existing compute was resized or deleted.

Updated the exact [ESv3 quota section](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.md#esv3-compute-sizes-and-quota-budget)
and its PDF, removing the old D16s_v3/64-core defaults and obsolete VM-quota image.
Optional development-instance examples now also use ESv3 and the configured region,
with their additional quota cost called out. The budget is **16 + 20 + 4 = 40 cores
per environment**, or **80** for colocated dev/prod before other workloads.

The 96 available Dedicated cores came from the user's screenshot; the existing CLI
context could not independently query that Azure ML subscription. The guide does
not claim live regional capacity. It explains total/workspace caps, online reserve,
the helper's skip-existing behavior, and existing-cluster migration requirements.
The actual project's `v2-creation` connection differs from the example YAML names;
authentication was not silently rebound. Compute identity/ACR access remains a
separate deployment prerequisite.

PDF validation: **34 pages**, **38 screenshots**, **3 expanded sections**, **35
headings**, **50 links**, and **11 quota-specific content checks** passed. The ESv3
table was visually reviewed; no broken images, missing anchors, local-file links,
or overflowing text were found.

- PDF size: **3,212,111 bytes**.
- PDF SHA-256: `f350c4cfb7536ba123a6578f420dc4827053e299e8794419f4c38b6572773467`.
- LF-normalized Markdown SHA-256: `40f93b2ec9ad7621c69dabed78fc0ca3bd456a57f9514f610aaf8777d0582198`.

## Follow-up: managed identities and ACR access before training

- Added [the managed-identity checklist](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.md#before-training-managed-identities-and-container-registry-access)
  directly before the training-pipeline section in the requested Markdown guide.
  Full infrastructure deployment is required; a preflight-only run creates no
  workspace, compute identity, or registry role assignments.
- Confirmed from the reusable Bicep that the workspace declares a system-assigned
  identity, while the compute module does not explicitly declare an identity or
  ACR role assignment. Explained that the compute, workspace, and Azure DevOps
  service-connection principals are separate; workspace grants do not transfer
  automatically to the training compute.
- Documented portal checks and registry-scoped role assignment to the actual
  compute principal: `AcrPull` for RBAC-only registries; `Container Registry
  Repository Reader` for ABAC-enabled registries, with catalog-list access only
  when required. Generic/configuration Reader access is separate, not a universal
  second image-pull requirement. Existing registry permission modes are not changed.
- Added least-privilege guidance, role-assignment authority, propagation/network
  checks, and separate workspace image-build, storage, batch, and endpoint identity
  considerations. The registry admin account remains disabled in the template.
- Linked the checklist from the setup notes and guide index, and repaired the
  indentation of all three optional compute-instance YAML examples. These are
  development instances, not substitutes for the training cluster identity.
- A focused standard/default read-only review approved the identity instructions.
  No custom Scrum agents, live Azure changes, managed-identity updates, RBAC grants,
  pipeline runs, or source/template runtime edits were performed in this task.
  The protected working taxi repository and unrelated local work are preserved.

The refreshed PDF contains **36 pages**, **38 screenshots**, **3 expanded sections**,
**40 headings**, and **54 external links**. All **14 identity-content checks** and
**3 YAML-example parsing checks** passed. The new section's placement before
training and its visual layout were checked; no broken images, missing anchors,
local-file links, or horizontal text overflow were found.

- [Updated PDF](https://github.com/alvinea28/own-ml-ops-v2/blob/main/documentation/deployguides/deployguide_ado.pdf)
- PDF size: **3,268,371 bytes**.
- PDF SHA-256: `a380638b8849891c10951540afc5f725438a558b2c24ccae595644e287c642c1`.
- LF-normalized Markdown SHA-256: `306c080555974595dafcb811e45b2d4d57e2eab5fa4464e3aaa2cd63b07d7c63`.

This export supersedes the earlier PDF metrics without changing live permissions.
